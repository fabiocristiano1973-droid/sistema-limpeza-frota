# Governo Interior não precisa de nenhuma variável de ambiente hoje — os
# dados vivem só no IndexedDB do dispositivo (privacy-first, sem backend).
#
# Este arquivo existe como ponto de extensão para o futuro, caso o app
# ganhe sincronização em nuvem (Supabase/PostgreSQL, conforme previsto no
# briefing original) ou autenticação própria. Nunca reutilize variáveis do
# sistema de limpeza da frota — são aplicações independentes.

# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
